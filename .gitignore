# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
.next
*.tsbuildinfo

# misc
.DS_Store
._*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.gstack/
output/

# gstack worktree copies
naughty-carson/
friendly-northcutt/
elated-montalcini/
goofy-merkle/
jolly-ritchie/
xenodochial-beaver/

# Claude Code 로컬 상태 파일 (ralph-loop 등) — 추적 금지
.claude/*.local.md

# git worktrees
.worktrees/

# Playwright MCP test artifacts
.playwright-mcp/

# Local visual QA captures should live in .codex-tmp/ or artifacts, not repo root.
/*.png
/*.jpg
/*.jpeg

# next-sitemap 이 postbuild 훅에서 자동 생성 — Vercel 빌드마다 갱신되므로 추적 금지
public/robots.txt
public/sitemap.xml
public/sitemap-*.xml

# 개인정보(전화번호, 연락처) 포함 운영 파일 — 푸시 금지
scripts/sms/
docs/2026캠프 운영/
docs/게스트초대_응답현황.xlsx
docs/테크라이더_취합.xlsx
docs/뮤지션_연락처.csv

# 스프레드시트 작업용 스크래치 디렉터리 — 추적 금지
.codex_spreadsheet_work/
.codex-tmp/
.vercel
supabase/.temp/

# codebase-memory-mcp (로컬 절대경로 — 환경별로 다름)
.mcp.json

# Noto 폰트 원본 ttf — 서빙 안 되는 서브셋 입력(총 100MB+). 추적 금지.
# scripts/fetch-noto-fonts.py 로 언제든 재현(google/fonts VF → 정적 추출).
# public/fonts/*.subset.woff2(서브셋 산출물)만 커밋된다.
src/fonts/source/Noto*.ttf

# Python 캐시
__pycache__/
